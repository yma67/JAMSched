file(GLOB_RECURSE JAMCOMPONENTS_SOURCES RELATIVE ${CMAKE_CURRENT_SOURCE_DIR}/ "${CMAKE_CURRENT_SOURCE_DIR}/*.c")
set(JAMCOMPONENTS_SOURCES_LIST "")
foreach(jamcomponent_source ${JAMCOMPONENTS_SOURCES})
    list(APPEND JAMCOMPONENTS_SOURCES_LIST ${jamcomponent_source})
endforeach(jamcomponent_source ${JAMTEST_SOURCES})
set(CMAKE_ASM_FLAGS "${CFLAGS} -x assembler-with-cpp")
list(APPEND JAMCOMPONENTS_SOURCES_LIST "${CMAKE_CURRENT_SOURCE_DIR}/core/ucontext/ucontext-impl.s")
add_library(libjamsched ${JAMCOMPONENTS_SOURCES_LIST})
target_include_directories(libjamsched PUBLIC ${PROJECT_SOURCE_DIR}/include)